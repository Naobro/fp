pip>=25.2
setuptools>=75.1.0
wheel>=0.44.0

streamlit==1.36.0

# 科学計算（Py3.13対応の安定レンジ）
numpy>=2.1,<3
pandas>=2.2.3,<3
matplotlib>=3.9,<3.10
numpy-financial==1.0.0

# PDF（3.x系はPy3.13非対応のため4系へ）
reportlab>=4.2,<5

# HTTP
requests>=2.31,<3

# supabase を使う場合のみ。使っていなければ **この行自体を削除**
# 使うなら存在する範囲に変更（uv/pipが解決できるようピンを緩める）
# supabase>=2.6.6,<3

# ── サブ依存は固定しない（Py3.13向けホイールを解決させる）
# pillow / kiwisolver / contourpy / fonttools / pyparsing / python-dateutil / packaging / tzdata などは削除
